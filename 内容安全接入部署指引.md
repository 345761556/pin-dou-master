# 内容安全接入部署指引（security.mediaCheckAsync · 云开发版）

> 适用项目：拼豆大师（图片转拼豆模板小程序）
> 目标：让"用户选图 / 换头像 / 上传图片"三个场景真正接入微信内容安全检测，过审不被拒。
> 本文件已在 `project.config.json` 的 `packOptions.ignore` 中，不会被打进正式包。

---

## 一、这套方案是什么

微信要求"用户可上传/发布内容的场景"必须接入内容安全 API（`security.mediaCheckAsync`）。该接口是**服务端接口**，小程序端拿不到、也不能存 `access_token`，必须有个后端代调。

本项目无自建后端，采用**微信云开发云函数 + 云调用**通道：
- 前端 `utils/secCheck.js`：压缩图 → 上传云存储 → `callFunction('secCheck')` → 拿 `suggest` → 放行/拦截。
- 云函数 `cloudfunctions/secCheck/index.js`：取云存储临时 URL → `cloud.openapi.security.mediaCheckAsync(version:2, media.media_url=url)` → 返回 `trace_id`。
- 检测结果由微信通过**消息推送**（`wxa_media_check` 事件）异步回调 `cloudfunctions/mediaCheckResult` 云函数，写入 `sec_check_results` 集合；前端轮询读取 `suggest`（pass/review/risky）。
- 通道异常（未开通云 / 网络 / 超限 / 推送未配）时前端 **fail-closed 拦截**（生产环境），仅 `develop` 本地调试 fail-open，保证核心功能不瘫痪。

**重要**：fail-closed 只保证"检测未完成的发布被拦截"，但若 **mediaCheckResult 未部署或其消息推送未配置**，检测结果永远写入不了 `sec_check_results` → 前端轮询超时后被拦截，**用户任何上传都无法成功**。提审前必须完成下面全部部署，否则审核会拒。

---

## 二、前置确认（你的项目已具备，无需再改）

| 项目 | 状态 |
|---|---|
| `project.config.json` 中 `cloudfunctionRoot: "cloudfunctions/"` | ✅ 已配置 |
| `cloudfunctions/secCheck/` 含 `index.js` / `package.json` / `config.json` | ✅ 已存在 |
| `cloudfunctions/mediaCheckResult/` 含 `index.js` / `package.json` / `config.json` | ✅ 已存在 |
| `utils/secCheck.js` 前端封装（体积守卫 / 云存储中转 / fail-closed 拦截） | ✅ 已完成 |
| `app.js` `onLaunch` 调 `_initCloud()`（`wx.cloud.init`） | ✅ 已接入 |
| 三处调用点 | ✅ index.chooseImage(scene=4) / profile.onChooseAvatar(scene=1) / profile.uploadPickerImage(scene=2) |

---

## 三、部署步骤（照做即可）

### 步骤 1：开通云开发

1. 打开**微信开发者工具**，进入本项目。
2. 点击顶部工具栏的**「云开发」**按钮（云朵图标，在预览/真机调试附近）。
3. 首次会弹「开通云开发」窗口 → 选环境（默认基础版即可）→ 开通。
4. 开通后出现云开发控制台，记下你的**环境 ID**（形如 `cloud1-xxxxxxxx` 或 `xxx-env-id`）。

**费用说明（2026 年口径）**：
- 可建 **1 个免费环境**（3000 资源点/月），开发与预览阶段免费。
- 正式发布上线后，免费环境通常转为付费基础套餐（约 **19.9 元/月**，原价 39，5 折长期有效）。套餐含 20 万次调用/月，拼豆大师用量极小，等于封顶价用不完。
- 云函数调用单价 **0.0133 元/万次**；内容安全接口本身有每日免费配额。整体基本白用。
- 对比自建后端（服务器+域名+SSL ≈ 200~555 元/年），云开发是最便宜方案。

### 步骤 2：确认云环境（单环境无需改代码）

`app.js` 当前 `wx.cloud.init()` **未指定 env**，会自动用默认环境（且为隐私合规刻意不传 `traceUser`）。

- **只开通了 1 个云环境** → 不用改代码，直接下一步。
- **开通了多个环境** → 必须显式指定，否则调用找不到环境：
  ```js
  // app.js _initCloud() 内
  wx.cloud.init({ env: '你的环境ID' });  // 注意：刻意不传 traceUser，避免同意前产生用户追踪
  ```
- 查看环境数量：云开发控制台左上角**环境下拉框**。

### 步骤 3：上传部署 secCheck 云函数（最关键、最易漏）

1. 开发者工具左侧「资源管理器」找到 `cloudfunctions/secCheck` 目录。
2. **右键该目录** → 选择 **「上传并部署：云端安装依赖」**。
   > ⚠️ 必须选「云端安装依赖」，不要选「不上传依赖」——云端没有 `wx-server-sdk` 会运行失败。
3. 等底部状态栏提示「上传成功」。
4. 验证：云开发控制台 → **「云函数」** → 能看到 `secCheck` 且状态正常、运行时为 Nodejs。

> 你之前报的 `-601034 没有权限，请先开通云开发或者云托管`，在云开发已开通的前提下，**绝大多数就是这一步没做**（函数没部署到云端）。

### 步骤 3.1：部署 mediaCheckResult 云函数并配置结果推送（关键一环，极易漏）

`secCheck` 只负责**提交**检测；**检测结果**由微信异步推送到 `mediaCheckResult` 云函数写入 `sec_check_results`，前端才能读到。这一步不做，前面的 secCheck 部署等于白做。

1. 开发者工具左侧资源管理器找到 `cloudfunctions/mediaCheckResult` 目录，**右键 →「上传并部署：云端安装依赖」**。
2. 配置微信**消息推送**（结果回调的来源）：
   - 云开发控制台 → **「设置」** → **「其他设置」** → **「消息推送」** → **「添加消息推送」**。
   - 推送模式：云函数
   - 消息类型：**event**
   - 事件类型：**wxa_media_check**
   - 云函数：**mediaCheckResult**
3. 验证：上传一张正常图，云开发日志应同时出现 `[secCheck] 检测完成 suggest=pass` 与 `[mediaCheckResult] 已写入结果`；若只看到 secCheck 提交、迟迟没有 mediaCheckResult 写入，说明推送未配 → 结果不可达，前端轮询超时后 fail-closed 拦截。

### 步骤 4：确认内容安全接口权限（云调用）

- `cloudfunctions/secCheck/config.json` 已声明：
  ```json
  {
    "permissions": {
      "openapi": ["security.mediaCheckAsync"]
    }
  }
  ```
- `mediaCheckAsync` 属微信**默认开放接口**，一般无需额外开通（官方答复：调用权限与企业认证状态、服务类目无关）。
- 若部署后调用仍报「无权限」：云开发控制台 → 设置 → 全局设置，确认相关开放接口可用；或在 mp 后台「开发 → 接口设置」核对内容安全接口状态。

### 步骤 5：创建持久集合（两个都要建）

`secCheck` 云函数有 openid 窗口限频（每小时每用户 100 次），持久计数用集合 `sec_check_rate`；`mediaCheckResult` 把检测结果写入集合 `sec_check_results`（`_id = trace_id`），前端轮询读取。

- **未建集合**：限频降级为「内存兜底」（单实例限频，不跨实例，功能不受影响）；`sec_check_results` 缺失则检测结果永不写入 → 前端轮询超时 fail-closed 拦截。
- **建议创建**（两个都要建）：云开发控制台 → 数据库 → 新建集合 → 名称 `sec_check_rate` 与 `sec_check_results`。

---

## 四、自检（发布 / 提审前必做）

1. **重新编译 → 真机预览**（必须真机/预览，编译期不会触发调用）。
2. 首页选一张**正常图片** → 生成模板：
   - 控制台应出现：`[secCheck] 检测完成 suggest=pass`
   - 同时应有：`[mediaCheckResult] 已写入结果 trace_id=...`
   - **不再出现** `[secCheck] 云开发通道不可用` 或 `图片检测调用失败 ... -601034`
3. **验证拦截**：上传/选择一张**公认违规图**，应被拦截并提示「图片内容含违规信息，请更换后重试」。
   > 注：当前云函数 catch 分支已统一收敛为 `sec_check_internal_error`，**无 87014 特判片段**，请勿照旧文档改代码。
4. 同样测 profile 页「更换头像」与「上传图片」两处。

---

## 五、常见报错对照

| 现象 | 原因 | 解决 |
|---|---|---|
| `[secCheck] 云开发通道不可用` | `wx.cloud` 未初始化 / 基础库过旧 | 确认 `app.js` `_initCloud` 执行、基础库 ≥ 2.33 |
| `图片检测调用失败 ... -601034 没有权限，请先开通云开发` | 云开发环境未开通 / `secCheck` 未部署 | 步骤 1 + 步骤 3 |
| 调用成功但 `suggest` 永远 unknown / `errcode` 非 0 | 云函数没部署成功 / openapi 权限缺 | 步骤 3 + 步骤 4 |
| 部署后函数报 `Cannot find module 'wx-server-sdk'` | 选了「不上传依赖」 | 重新右键 →「上传并部署：云端安装依赖」 |
| 提交成功但**轮询一直 pending / 超时后被拦截** | `mediaCheckResult` 未部署 或 `wxa_media_check` 推送未配置 | 步骤 3.1 |

---

## 六、提审清单

- [ ] 云开发已开通
- [ ] `secCheck` 已「上传并部署：云端安装依赖」
- [ ] `mediaCheckResult` 已「上传并部署：云端安装依赖」
- [ ] 已配置 `wxa_media_check` 事件推送（指向 mediaCheckResult 云函数）
- [ ] 选正常图 → 控制台 `suggest=pass`、无 `-601034`、且有 mediaCheckResult 写入日志
- [ ] 违规图 → 被拦截提示「含违规信息」
- [ ] （建议）已建 `sec_check_rate` 与 `sec_check_results` 两个集合
- [ ] 三调用点均接入（index 选图 / profile 头像 / profile 上传图）
