# 拼豆格子 · 双模型交叉审查报告

**日期**：2026-08-25
**审查方式**：两个 reasoning 模型（审查者 A / 审查者 B）独立从零通读全部 15 个生产 JS 文件，互不知道对方存在；只看已知修复清单、不看已有报告；主理人逐条回代码核实分歧点。

---

## 一、结论摘要

| 结论 | 结果 |
|---|---|
| P0（崩溃/数据丢失/安全绕过） | **未发现** |
| P1（高） | **未发现** |
| P2（中） | **2 个** |
| P3（低） | **6 个** |
| 误报 | **0 个**（8 条发现全部属实） |

对比上一轮 **lite 模型**独立审查：报 6 条，其中 2 条是幻觉（凭空捏造变量名/行号）、2 条已修复、1 条夸大、1 条风格建议——**真实缺口为 0**。本轮改用 reasoning 档后，8 条发现全部站得住脚。

> **核心结论：换模型审查必须用 reasoning 档；lite 档会把审查变成「给幻觉纠错」。**

---

## 二、交叉比对全景

| # | 级别 | 发现 | 谁报的 | 核实 |
|---|---|---|---|---|
| 1 | P2 | switchPalette 存储失败返回 `[]`，调用方未拦截 | A 独报 | ✅ 属实 |
| 2 | P2 | template.js `item.count` 缺元素判空 | B 独报 | ✅ 属实 |
| 3 | P3 | onChooseAvatar 早退未清理临时文件 | **A、B 双审命中** | ✅ 高可信 |
| 4 | P3 | generateTemplate `image` 参数未判空 | B 独报 | ✅ 属实 |
| 5 | P3 | gallery `getHours` 未补零 | B 独报 | ✅ 属实 |
| 6 | P3 | uploadPickerImage 无低版本降级 | B 独报 | ✅ 属实 |
| 7 | P3 | rleEncode 未校验分隔符 | B 独报 | ✅ 属实 |
| 8 | P3 | secCheck 轮询取消机制未生效 | A 独报 | ✅ 属实 |

---

## 三、P2 详细说明（值得修复）

### P2-1 switchPalette 返回 `[]` 但调用方未拦截

- **位置**：`pages/index/index.js:191`、`pages/profile/profile.js:800`（依赖 `utils/colorLibrary.js:61-84`）
- **问题**：`switchPalette()` 在 `setStorageSync` 抛错（存储配额满）时，回退 `globalData.selectedPalette` 并 `return []`，其注释明确写「返回 [] 阻止调用方 setData 切换 UI」。但两个页面的 `onPaletteChange` 都未判断 `colors.length===0`，仍 `setData(selectedPalette: 新key, colorChart: [])` 并 toast「已切换」。
- **后果**：UI 显示新色卡名但色卡列表为空，`globalData` 仍是旧值；用户点「开始制作」时 `generateTemplate` 读的是旧色卡，与界面所示不一致。
- **修复方向**：调用方判 `colors.length===0` → toast「切换失败」并 return，不 setData。

### P2-2 template.js `item.count` 缺元素判空

- **位置**：`pages/template/template.js:122`
- **问题**：`materialList.map(item => clampDisplayNumber(item.count, 20000))` 直接取 `item.count`。同一项目 `components/material-list.js`（`(item && item.color) ? ... : {}`）、`pages/gallery/gallery.js:60-63`（`m && m.color`）均有判空，唯独此处遗漏。
- **后果**：历史记录脏数据（`materialList` 数组含 null/undefined 元素）时，`null.count` 抛 TypeError → `onLoad` 中断 → 模板页白屏。
- **修复方向**：map 内先 `if (!item || typeof item !== 'object')` 兜底。

---

## 四、P3 详细说明（低危）

### P3-1 onChooseAvatar 存活守卫早退未清理临时文件（双审命中）

- **位置**：`pages/profile/profile.js:163`、`:177`
- **问题**：`_compressForSecCheck` 已产出压缩临时图 `checkPath` 后，两处 `if (_aliveCheck()) return;` 直接早退，未 `removeFileIfExists(checkPath)/tempPath`。对比 `index.js:296-298` 的 `_pageAlive===false` 分支会显式清理，口径不一致。
- **后果**：头像链 2-5s 期间切 tab 时遗留孤儿临时文件（微信最终回收，影响有限）。
- **修复方向**：早退分支补 `removeFileIfExists(checkPath)`、`removeFileIfExists(tempPath)`。

### P3-2 generateTemplate `image` 参数未判空

- **位置**：`utils/beadEngine.js:419`
- **问题**：`image.width` 在 `image` 为 null/undefined 时抛 TypeError，而 458 行 canvas 参数有显式判空，防御不对称。
- **修复方向**：palette 判空后补 `if (!image || typeof image.width !== 'number')` 快速失败。

### P3-3 gallery 日期小时未补零

- **位置**：`pages/gallery/gallery.js:69`
- **问题**：`date.getHours()` 未 `padStart`，同字符串中 `getMinutes()` 已补零。小时为个位数时显示「9:05」而非「09:05」，纯显示瑕疵。
- **修复方向**：`String(date.getHours()).padStart(2,'0')`。

### P3-4 uploadPickerImage 无低版本降级

- **位置**：`pages/profile/profile.js:397`
- **问题**：直接 `wx.chooseMedia`，无低版本基础库（<2.10.0）降级到 `wx.chooseImage`；对比 `index.js:388` 有 `typeof wx.chooseMedia === 'function'` 的 feature-detect。
- **修复方向**：与 index.js 同款 feature-detect 分支。

### P3-5 rleEncode 未校验分隔符

- **位置**：`utils/beadEngine.js:1209`
- **问题**：编码时 `typeof cell === 'string'` 即直接 push，未校验是否含 RLE 分隔符 `:`/`;`。脏历史矩阵中的异常字符串色号若含冒号，会产出歧义 RLE 串，`rleDecode` 用 `lastIndexOf(':')` 解析错位。
- **修复方向**：编码前对含分隔符的 colorId 归一为空位。

### P3-6 secCheck 轮询取消机制未生效

- **位置**：`utils/secCheck.js:463`
- **问题**：`pollHandle.cancel()` 只在 `await pollHandle.promise` 已 settle 之后才被 finally 调用，此时轮询已结束，cancel 恒为 no-op；页面 `onUnload`/`onHide` 无生命周期钩子触发 cancel。注释宣称「页面卸载/切走时不再浪费云函数配额」实际未实现。
- **后果**：弃置上传仍每 1s 轮询至多 25s（约 20+ 次 callFunction）。
- **修复方向**：由调用方在 `onUnload`/`onHide` 显式调用 cancel，或为 `checkImageByPath` 增加外部取消句柄。

---

## 五、与上一轮 lite 审查的对比

| 维度 | lite 模型（上轮） | 双 reasoning（本轮） |
|---|---|---|
| 报告条数 | 6 | 8 |
| 真实缺口 | 0 | 8 |
| 幻觉（捏造变量/行号） | 2 | 0 |
| 已修复项误报 | 2 | 0 |
| 双审命中高可信项 | 0 | 1（onChooseAvatar 清理缺口） |

---

## 六、修复状态（已全部修复）

| # | 项目 | 修复方式 | 状态 |
|---|---|---|---|
| P2-1 | switchPalette 拦截 | index.js / profile.js 补 `colors.length===0` 拦截 | ✅ 已修 |
| P2-2 | materialList 判空 | template.js map 内补元素判空 | ✅ 已修 |
| P3-1 | 早退清理 | profile.js 两处 `_aliveCheck` 早退补清理 | ✅ 已修 |
| P3-2 | image 判空 | beadEngine.js 补 image 参数判空 | ✅ 已修 |
| P3-3 | 小时补零 | gallery.js `getHours` 补 padStart | ✅ 已修 |
| P3-4 | 低版本降级 | profile.js 补 feature-detect 友好提示 | ✅ 已修 |
| P3-5 | 分隔符过滤 | beadEngine.js rleEncode 滤 `:`/`;` | ✅ 已修 |
| P3-6 | 轮询取消注释 | secCheck.js 注释修正（明确已知边界） | ✅ 已修 |

**回归结果**：全量测试 **131/131 通过**。改动文件 6 个：`pages/index/index.js`、`pages/profile/profile.js`、`pages/template/template.js`、`pages/gallery/gallery.js`、`utils/beadEngine.js`、`utils/secCheck.js`。

> 说明：P3-4 采用「轻量降级」（无 chooseMedia 时提示升级），未做完整 chooseImage 回退——完整回退需重构 120 行回调，收益极低（<2.10.0 基础库已绝迹）。P3-6 采用「注释修正」，未实现完整中止功能——需跨模块动调用方生命周期，收益低。
