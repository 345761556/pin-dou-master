# 拼豆格子 · 通篇代码审查报告

**审查日期**：2026-08-25
**审查范围**：15 个生产 JS 文件，约 8000 行（app.js / utils×6 / pages×4 / components×2 / cloudfunctions×2）
**审查方法**：主审查人逐文件精读 + 独立 reasoning 审查 agent 并行交叉验证

---

## 一、核心结论

**未发现 P0（崩溃/数据丢失/安全绕过）与 P1 级缺陷。**

代码库经历过密集的多轮审查迭代（注释中可见"第一~十六轮审查""外部审查 #N""P1-1/P2-2 修复"等标记），防御性编程水平极高。发现的问题集中在：

| 严重度 | 数量 | 说明 |
|---|---|---|
| P0 | 0 | 无 |
| P1 | 0 | 无 |
| P2 | 1 | 竞态/状态污染缺口（可复现） |
| P3 | 8 | 死字段、注释不一致、防御纵深、低危清理 |

---

## 二、P2 发现

### [P2-1] `uploadPickerImage` 异步链缺少 `_pageAlive` + `_pickerSession` 双重守卫

- **位置**：`pages/profile/profile.js` 第 389-527 行（chooseMedia success 异步回调）
- **问题**：该回调整条异步链（压缩 → secCheck → getImageInfo → setData，耗时 2-5 秒）只检查 `this._colorPickerClosed`，从不检查 `_pageAlive` 和 `_pickerSession`。而同文件的 `pickColorAtPoint`（第 658/674/689/703 行）三重守卫齐全，`onChooseAvatar` 有 `_aliveCheck()`。
  - **缺口 1（隐藏页 setData）**：profile 是 tabBar 页，`onHide` 只置 `_pageAlive=false` 不置 `_colorPickerClosed`。用户检测期间切走 tab，迟到的 `setData({pickerImagePath...})` 仍写到隐藏页（触发告警、污染状态）。
  - **缺口 2（关后立刻重开竞态）**：`showColorPicker` 会复位 `_colorPickerClosed=false` 并递增 `_pickerSession`，但 upload 回调只看 `_colorPickerClosed` 不看 `_pickerSession`。选图→检测中→关闭弹窗→立即重开→旧 session 迟到的 success 看到 `closed=false` 继续 setData，把上一轮旧图写入新打开的弹窗。
- **建议**：在各 await 之间补 `_pageAlive` 判断（对齐 `onChooseAvatar` 的 `_aliveCheck`），写 picker 状态前比对入口快照的 `_pickerSession`，不一致即静默清理返回。
- **修复代价**：小（补几处守卫判断，与同文件其他链路口径一致）。

---

## 三、P3 发现

| # | 位置 | 问题 | 性质 |
|---|---|---|---|
| P3-1 | `app.js:278/288/291/476` | `globalData.cloudAvailable` 死字段，全项目只写不读（`secCheck.isCloudAvailable` 独立探测 `wx.cloud.uploadFile`） | 死代码/维护地雷 |
| P3-2 | `utils/secCheck.js:351-363` | `checkImageByPath` 入口只判空串/非字符串，未调用 `isValidFilePath`（与 `saveImageToAlbum`/`canvasToImage`/`compressImageIfNeeded` 口径不一致；当前靠 `getFileInfo` 失败 fail-closed 间接兜底） | 防御纵深缺口 |
| P3-3 | `cloudfunctions/mediaCheckResult/index.js:59 vs 64` | 第 59 行注释"生产默认 3000ms"与第 64 行代码 `return 500` 矛盾（P3-9 修复改默认值后注释未同步） | 注释不一致 |
| P3-4 | `pages/index/index.js:769 vs 782` | 注释称"recordId 与 record.id 同一真源"，实为两次独立 `Math.random()` 调用 | 注释不实 |
| P3-5 | `pages/template/template.js:482` | `_getExportCanvas` 重试定时器未在 onUnload 清理（有 `_destroyed` 收敛，无崩溃，仅短时浪费查询） | 低危清理 |
| P3-6 | `pages/index/index.js:300-303` | 换图成功后旧图临时文件不清理（与失败路径 `removeFileIfExists` 口径不一致，靠系统回收） | 一致性 |
| P3-7 | `utils/beadEngine.js:1257-1261` | `rleDecode` 空串分支返回受 `CELLS_HARD` 限制的大矩阵（约 1.6 万格 null），对"无数据"空串属无谓分配 | 内存优化 |
| P3-8 | `utils/security.js:12-13` | 注释称"非微信环境空串前缀匹配恒 false"，但 `''.startsWith('')` 实为 true（仅影响 Node 单测环境，微信端 `WX_USER_DATA_PATH` 恒非空） | 注释勘误 |

---

## 四、健壮性专项确认（已验证到位，无需改动）

- **异步错误隔离**：`chooseImage`/`onChooseAvatar`/`uploadPickerImage` 的 async success 回调均有顶层 try-catch 兜底（H1 修复）；轮询有 `{promise, cancel}` + finally 取消
- **竞态防护**：限频已原子化（`_.inc`+`_.lt`）；`_measuring`/`_generating`/`_pickerBusy`/`_saveBusy`/`_shareBusy`/`_avatarBusy`/`_savingBusy` 互斥完整
- **资源清理**：云存储中转文件删除覆盖前端四路径 + 云函数 submit 失败 + mediaCheckResult 成功路径；孤儿原图回收、`gcBeadTempFiles` 启动兜底
- **边界钳制**：`clampTemplateSize`/`rleDecode`/`renderTemplate` 对空数组、NaN/Infinity、超 4096/20000 维度、脏历史数据均有钳制
- **fail-closed**：`secCheck` 图片/文本两链路失败策略一致（仅 develop fail-open，其余环境拦截）
- **内存/性能**：`rgbToLab` 缓存有界（50000 清空）、Lab 缓存 freeze 防污染，无无界增长或明显 O(n²) 热点

---

## 五、修复建议（按性价比排序）

**建议本轮修复（代价小、对齐项目高标准）**：
1. **P2-1**（唯一有实际竞态风险的真实缺口）：`uploadPickerImage` 补 `_pageAlive` + `_pickerSession` 守卫
2. **P3-2**：`checkImageByPath` 入口补 `isValidFilePath` 校验（一行）
3. **P3-3 / P3-4 / P3-8**：修正注释（零代价）

**可排期（影响极小）**：P3-1 死字段删除、P3-5 定时器清理、P3-6 换图清理、P3-7 空串矩阵返回 `[]`

---

## 六、修复状态（2026-08-25 完成）

| 项 | 状态 | 说明 |
|---|---|---|
| P2-1 | ✅ 已修复 | `uploadPickerImage` 补 `stale()` 守卫（`_pageAlive`+`_pickerSession`+`_colorPickerClosed` 三合一） |
| P3-1 | ✅ 已修复 | 删除 `cloudAvailable` 死字段（4 处） |
| P3-2 | ✅ 已修复 | `checkImageByPath` 入口补 `isValidFilePath` |
| P3-3 | ✅ 已修复 | 注释 3000ms → 500ms |
| P3-4 | ✅ 已修复 | 注释"同一真源"→"各自独立随机" |
| P3-5 | ✅ 已修复 | `_getExportCanvas` 重试入口补 `_destroyed` 检查 |
| P3-6 | ✅ 已修复 | 换图成功后清理旧 imagePath 临时文件 |
| P3-8 | ✅ 已修复 | 注释"恒 false"→准确描述 |
| P3-7 | ⏸️ 跳过 | 涉及 rleDecode 返回值语义，需动调用方，保守未改 |

**回归测试**：全量 131/131 通过（`node test/_run_all.js`）。

**附带修正**：恢复误删的 `_gen_icons.py`（被 git 跟踪且有 2 个测试依赖）。
